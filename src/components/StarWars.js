import React from 'react';
import {starWarsInfo} from "../utils/Constants";

const StarWars = () => {
    return (
        <div>
            {starWarsInfo}
        </div>
    );
};

export default StarWars;